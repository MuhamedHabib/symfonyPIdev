<form method="post" action="{{ path('reclamation_delete_User', {'idReclamation': reclamation.idReclamation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.idReclamation) }}">
    <button class="btn btn-primary btn-sm mt-3 mb-3" style="background-color: #761c19; border-color: #761c19">Supprimer</button>
</form>
