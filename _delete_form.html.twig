<form method="post" action="{{ path('message_delete', {'idMessage': message.idMessage}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.idMessage) }}">
    <button class="btn">Delete</button>
</form>
