<form method="post" action="{{ path('events_delete', {'idEvent': event.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.idEvent) }}">
    <button class="btn">Supprimer</button>
</form>
